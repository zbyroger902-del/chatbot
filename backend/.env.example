# Copy to .env and fill in. Do not commit .env.
# JWT_SECRET must be at least 32 characters (e.g. openssl rand -base64 32)
JWT_SECRET=
DATABASE_URL=postgresql+asyncpg://chatbot:chatbot@localhost:5433/chatbot
